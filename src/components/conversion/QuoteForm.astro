---
interface Props { action?: string; method?: 'post' | 'get' }
const {action = '/api/submit-inquiry', method = 'post'} = Astro.props as Props
---
<form action={action} method={method} class="quote-form">
  <slot />
</form>
<style>
  .quote-form { display:grid; gap:12px; }
</style>

---
interface Props { steps: string[] }
const {steps} = Astro.props as Props
---
<ol class="steps">
  {steps.map((step) => <li>{step}</li>)}
</ol>
<style>
  .steps { margin:0; padding-left: 20px; display:grid; gap:8px; color:#334155; line-height:1.7; }
</style>
